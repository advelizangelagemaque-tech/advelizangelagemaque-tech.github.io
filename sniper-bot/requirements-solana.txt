# Dependências do módulo experimental Solana (instale só se for usá-lo).
# As checagens de segurança (safety/rpc/wallet) NÃO precisam destas libs.
solders>=0.21.0          # assinatura de transações
websockets>=12.0         # detecção automática (logsSubscribe)
